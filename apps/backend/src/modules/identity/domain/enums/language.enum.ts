// Finite set for V1 (Arabic-first, per the platform's localization principle).
// Additional languages can be introduced here as the platform expands.
export enum Language {
  Arabic = 'AR',
  English = 'EN',
}
