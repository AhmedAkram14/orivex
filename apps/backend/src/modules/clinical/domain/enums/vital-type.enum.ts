// Lowercase-hyphen string values matching the frontend's real `VitalType`
// contract exactly (apps/frontend/src/features/patient/api/types.ts).
export enum VitalType {
  Weight = 'weight',
  BloodPressure = 'blood-pressure',
  BloodSugar = 'blood-sugar',
}
